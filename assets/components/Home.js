import React from 'react';
import AutoComplete from './AutoComplete';

const Home = () => {
    return (
        <div className="home">
            <AutoComplete />
        </div>
    );
};

export default Home;
